#include<stdio.h>
int main(){
    int numeroInical;
    int numeroFinal ;
    int somaPares=0;


    printf ("digite um numero incial positivo:");
    scanf("%d", &Inicial);

    while(fim <=inicial);

    for(int i = inicio; i <= fim; i++);{
        if( i % 2= 0){
            somaPares = i;
    }

    printf ("digite um numrero final deve ser maior que %d :" inicial);
    scanf("%d" ,&final);

    printf("imprima a soma dos pares ao final:");

    return 0;
}